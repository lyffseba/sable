class_name OutfitStyle
extends RefCounted

## Albedo ids for a later paint pass. Same cut / skeleton as the wear-this capsule.

var id: String = ""
var body: Color = Color(0.14, 0.145, 0.155)
var stitch: Color = Color(0.90, 0.88, 0.82)
var chest_band: bool = true
var chest_mint: bool = false
var rust: Color = Color(0.55, 0.28, 0.18)
var rust_visible: bool = true
var stripe_only_on_lift: bool = false
