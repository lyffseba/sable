# Modes / playlist

**The Yard** (original paintball range, `docs/yard.md`) is the **sole active map**. Salt House gallery runs on the Yard. Bay booth is **parked** — code and scenes stay; player chrome does not offer it.

Internal house phase stays `range` (`play("range")`, `setPhase("range")`). The player-facing mode name is **GALLERY**. Do not rename the phase to trap lift/HID or kill Offline.

Durable hangar session is `S.hangar`: `hangar` (boot / Offline gallery) | `wait_practice` (HUD-on-Yard waiting + WARM UP) | `match_live` (shared ENTER RANGE). It is a thin HUD seam so waiting-practice and shared gallery are not overloaded on `lobby` / `range` alone. Screen/sim phase stays `boot` / `lobby` / `lock` / `calibrate` / `range` / `results`. Server room `phase` stays `wait` | `range` | `bay`.

**SableNet hangar lock:** the room owns that session class (`wait`→`wait_practice`, `range`→`match_live`; Bay stays parked). Practice never promotes the room — only ENTER RANGE writes `match_live`. Poll / snapshot is a view of the enum. Offline Range and WARM UP stay client-local parks (no wire gate). Lift/HID never waits on a hangar write. Fail loud if fire gates on hangar or Offline / WARM UP talk to the room for hangar.

| Entry | Playlist | Rules | Soft-lock |
|-------|----------|-------|-----------|
| **OFFLINE** | local gallery (`play("range")`) | 60 s clock, score, ESC = miss, `GALLERY CLEAR` | one click, local |
| **WARM UP** | local gallery, seat stays | same house, practice — RETURN TO LOBBY | one click, no `/api/lobby/start`; phase-preserve the live Yard — no `play()` lock tax |
| **ENTER RANGE** | shared Salt House / Yard | same 60 s gallery, room owns plates + sit / flyer pose + SCORE | host shares the live Yard; phase-preserve — skip calib/lock when already lifted |

ONLINE is Yard always-practice + shared gallery. Do not paint 5v5, ALPHA/BRAVO, or playlist id 5v5. Kicker / session label stay Yard / GALLERY / SHARED.

Sit / flyer pose is closed-form from life (`sitPoseY` / `sit_pose_y`: bob then drop; `flyerPose` / `flyer_pose`: `y0 + vy0*t - 0.5*g*t^2`). Hitscan is the same sphere (`hitscanRange` / `_hitscan`). Offline / WARM UP / wait_practice local plates and match_live rewind share that house. Do not Euler-integrate flyers. Do not mesh-test the spun hex — a HID peek that hits the plate must hit the rewind sphere. match_live plate life is `(committedSimMs() - born_ms)` — the same clock `_pose_at` rewinds at `fire_ms`. A local `o.life += dt` after a poll must not invent the sphere. The room owns seed + hits + SCORE (combo / worth) + shots / ACCURACY + combo_max / COMBO + the 60 s bell + ROUND remaining only after ENTER RANGE. Seed owns kind / peek / velocity / `born_ms` — a late first poll must not shift the house. ESC is a room miss for combo. ESC is not a shot. ESC does not rewrite the peak. `applySharedSim` snaps the book, then GALLERY CLEAR. Practice never opens the shared sim. A local peek must not keep phantom points — two friends must not both credit the same plate. A shared escape must not leave combo live. A local `simMs()` must not close match_live or paint ROUND. A local `S.shots++` must not invent ACCURACY. A local `if (S.combo > S.comboMax)` must not invent COMBO.

Bay (local first-to-5 / shared booth) is parked. `startBay` / `lobbyStartBay` / `house.js` booth may remain; boot `btn-bay` and lobby **ENTER BAY** stay off player chrome.

## Gallery rules (Salt House)

- Round clock is `RANGE_MS` (60 s) on the 128 Hz sim (`galleryOver` / `galleryLeftMs`). Offline / WARM UP close locally. match_live snaps `over` and ROUND remaining from the room (`S.elapsedMs`) — a `fire_tick` at or past the bell does not credit. A local `simMs()` must not paint shared remaining.
- Score + combo are the mode. Escape (`ESC`) is a miss — Offline / WARM UP drop combo locally; match_live drops the room book (`_gallery_escape`) so two friends agree. Sit plates dwell, then drop. Clays leave.
- End state is **GALLERY CLEAR** (score / hits / accuracy / combo / 60 s round). match_live GALLERY CLEAR is the room bell, not a per-client `simMs()`. match_live ACCURACY is room hits / room shots — not a local `S.shots++`. match_live COMBO is room `combo_max` — not a local `if (S.combo > S.comboMax)` from a poll.
- Offline HUD session is `GALLERY`. WARM UP stays `WARM UP`. Shared house stays `SHARED`. ONLINE session is never `5v5`.
- Arcade feedback is a thin **SableHUD** bar (`SABLE_HUD_H` 22px): hangar chip from `S.hangar` (`WAIT` / `READY` / `LIVE`), a thin `ROOM` chip from `S.room` when `wait_practice` or `match_live`, then `SCORE`, `ROUND` + time remaining, end chip (`60s GALLERY` / session / `GALLERY CLEAR`). Thin MAG chip after CONF in `drawModeChip` on the hand/GUN path only — same 22px row (`MAG n` bone/mint; rust `DRY` at 0). Mag tell = reload honesty. Thin SAFE chip after CONF (with MAG) when pointing + thumb-parallel — SAFE tell = thumb-parallel honesty. Additive — do not rename `S.mode`. Do not invent MAG under DESKTOP. DESKTOP / forceGun do not invent a pad mag story. Empty shark-fin stays missTick. match_live CLEAR snaps from room `over` (`S.over`) — do not invent CLEAR from local `galleryLeftMs(simMs())` while still on range. match_live ROUND remaining snaps from room `elapsed_ms` (`galleryHudLeftMs` / `S.elapsedMs`) — do not invent remaining from local `simMs()`. Offline / WARM UP still close locally. Charcoal plate, bone / mint / rust ink. No bloom, no tutorial wall, no paint over the cuff or reticle. Do not thicken the lobby. Waiting arena stays HUD-on-Yard (`startWaitingYard`) — local plates, thin `WAIT` + `ROOM` chips, no gallery SCORE / ROUND, no 60 s lock, no net on the click. WAIT never mutates the local score book (`S.score` / hits / shots / combo) and never pushes point / ESC popups — WARM UP (`phase === "range"`, `S.warmup`) stays fully scored; `startRange` on promote still resets the book. A fresh ONLINE arms the camera fire-and-forget (`armPracticeCam`) so lift / shark-fin / HID can live — no lock/calib tax. Camera deny arms desktop on that live lobby (`armPracticeDesktop`) — do not dump into `goDesktopRange`. **Q4 fail-to-lock is SEEKING until lock or Space** — never OS cursor, never auto-desktop when `camReady` and no hand. Thin SableHUD SEEKING chip on `wait_practice` (waiting Yard / WARM UP) so SEEKING is not silent. Camera deny is not fail-to-lock. Overlay `ROOM` stays; the HUD chip is additive so friends see the code without thicker chrome.
- Sparse **SableAudio**: dry-tick miss (escape / dry fire) and hit punch on shatter. Quiet mint-tell lift cue (`Mint. Lift.` — oscillator this cut) after GUN — waiting-Yard `lobby` is live for mint-tell, same `afterLiftState` hysteresis as range. `armPracticeDesktop` invokes `afterLiftState` after writing DESKTOP truth. `enterGame` invokes `afterLiftState` after `setPhase`. `Space` forceGun invokes `updateMode` before `afterLiftState`. `drawModeChip` does not prefer SEEKING when `S.forceGun` or `S.mode === "GUN"`. Audio only — do not paint VO over the cuff. Feedback after resolve / lift state, never a fire gate. No bed, no ambience.
- Stand still. WASD is Bay only (parked booth).

## Bay rules (parked 1v1 booth)

Parked. Not playable from boot or the waiting arena. Specs stay so the booth can return later.

- First to 5 (`BAY_TO_WIN`). Pose / expose / freeze live on the 128 Hz sim (`tickBay(SIM_DT)` from `stepSim`). Hitscan peeks the last committed pose.
- Fire is HID (`fire()` → `fireBay3D`). Stamps `Bay.fireMs = committedSimMs()`. Soft-parked `lobbyStartBay` fire-and-forgets `/api/lobby/bay` — never `/api/lobby/start` (that starts the house). Shared resolve is a lazy snapshot + fire-tick rewind. `reportSharedBayFire` / pose / lobby poll stay after `SablePerf.markHid` — HID→hitscan p99 < 8 ms still holds. No 128 Hz friend loop.
- Room-owned shared Bay (parked): last committed pose mailbox + peeked UV + `fire_ms` on the 128 Hz grid. The room ray-tests that UV against the foe capsule at that tick. A miss stays a miss. Open-middle death is an expose intent the room verifies.
- Arcade feedback is the same thin **SableHUD** bar: `YOU` / `THEM` / `ROUND` + cover chip + `FIRST TO 5` / `MATCH`. Charcoal plate, bone / mint / rust ink. No bloom, no tutorial wall, no paint over the cuff.

## Geometry

CS / Fortnite layout literacy is **architecture notes only** (`docs/port.md`, `research/`). Fortnite-class on the Look bar is **silhouette literacy only** (charcoal / bone / mint / rust). Runtime Salt House / Yard / Bay is original SABLE geometry. Zero Valve / Epic / UEFN asset DNA — no `de_*` halls, no Marketplace packs, no third-party map files.

## Port path

SablePort owns later-migrate notes. Product verb is AimBus hand gesture (point + shark-fin peek; charger-plug reload) — DESKTOP HID is honesty fallback. Sim stays 128 Hz with fire outside. Look bible stays charcoal / bone / mint / rust. Playlist stays this file. Runtime host is SABLE; ship floor is Chromium on MacBook Pro–class — `proto/port.js` is identity only. Destinations named in `docs/port.md` are feeling / architecture, not content.

## Do not

- Do not offer Bay from boot or lobby. The Yard is the sole active map.
- Do not wait on a camera frame, the Hands worker, or the 128 Hz step to fire. Do not bind HID to `canvasHUD` — `#hud` is `pointer-events: none` and mutes the pad. Window `onHidPointerDown` peeks; chrome still owns WARM UP / ENTER RANGE / LEAVE. After JOIN, leftover CODE/JOIN under the hidden cursor must not eat the pad (`muteJoinPad`). `onHidPointerDown` publishes click UV when DESKTOP owns the mailbox — first pad after cam deny / T / `goDesktopRange` must not peek `{0.5,0.5}`. Cam / GUN / shark-fin must not publish on the click. `forceGun` re-arms pad as Q4 emergency only; Space is not a shot; `productGunHidFire` stays false. `updateMode` writes DESKTOP truth (`seeking` false, `lifted` true) even if `!camReady`. `syncCursor` shows the OS cursor when `S.desktop`. `draw2D` skips `drawCrosshair` when `S.desktop`. `publishAim` writes confidence 1 when `S.desktop`. `armPracticeDesktop` invokes `afterLiftState` after writing DESKTOP truth. `enterGame` invokes `afterLiftState` after `setPhase`. `Space` forceGun invokes `updateMode` before `afterLiftState`. `drawModeChip` does not prefer SEEKING when `S.forceGun` or `S.mode === "GUN"`.
- Do not bloom the reticle. Charcoal / bone / mint / rust only. Yard bunkers stay charcoal / rust (few, low) so bone plates read.
- Do not hide the gun with HUD copy. SableHUD stays a thin top bar over live aim.
- Do not book SCORE / hits / shots / combo or paint point / ESC popups on `S.waitingYard`. WAIT is always-practice; WARM UP / Offline / match_live books stay. `startRange` on promote still resets the book.
- Do not thicken the lobby. Waiting-arena chrome stays WARM UP / ENTER RANGE (+ LEAVE). The waiting arena is HUD-on-Yard always-practice — live plates, thin `WAIT` + `ROOM` chips from `S.hangar` / `S.room` — not a match-start screen. Do not paint 5v5 / ALPHA / BRAVO / playlist id 5v5. Do not paint hangar chips from screen phase; read `S.hangar` only. Do not hide the gun with a ROOM chip. Do not leave the waiting Yard on a dead gun. `setPhase("lobby")` arms the camera fire-and-forget (`armPracticeCam`) — no lock/calib. Camera deny arms desktop on that lobby (`armPracticeDesktop`) — do not `goDesktopRange` off the waiting arena. `updateMode` writes DESKTOP truth (`seeking` false, `lifted` true) even if `!camReady`. Do not invent OS-cursor aim on waiting-Yard fail-to-lock. **Q4 fail-to-lock is SEEKING until lock or Space** when `camReady` and no hand — never OS cursor, never auto-`goDesktopRange` / `armPracticeDesktop`. Camera deny is not fail-to-lock. Offline lock timeout → `goDesktopRange` stays Offline. Thin SEEKING chip stays on the 22px bar — do not hide the cuff, do not grow a tutorial wall. Space `forceGun` is the Q4 escape. `Space` forceGun invokes `updateMode` before `afterLiftState`. `drawModeChip` does not prefer SEEKING when `S.forceGun` or `S.mode === "GUN"`. KeyT stays debug. KeyT must not disarm DESKTOP on cam-deny waiting Yard (`lobby` / `wait_practice`) — re-arm via `armPracticeDesktop`. When `camReady`, KeyT debug toggle-off stays.
- Do not force calib/lock on ENTER RANGE when the Yard is already live. Promote is SableNet phase-preserve (`enterRangePreserve`) — lift/HID never waits on the lobby POST.
- Do not force calib/lock on WARM UP when the waiting Yard is already live. `lobbyWarmup` phase-preserves (`alreadyLifted` / `lobby` / `range`) — `play()` lock would `resetLockState` and disarm the gun `#68` armed. Cold path still `play("range")`. `alreadyLifted` treats DESKTOP as live without `camReady`; phase-preserve still skips `play()` lock.
- Do not collapse `wait_practice` and `match_live` back onto `lobby` / `range` alone. `S.hangar` is the durable session enum (`assignHangar` / `syncHangar`).
- Do not let a client invent hangar for a shared room. The room snapshot owns hangar.
- Do not promote hangar from WARM UP / practice. Only ENTER RANGE writes `match_live`.
- Do not touch `AimSample`. Fire peeks `AimBus` only.
- Do not let shark-fin or reload rewrite aim. `maybeSharkFinFire` peeks the last pointing UV after `updateMode` and before `updateAim`. Thumb UP is product fire; thumb parallel is safe. Do not re-gate shark-fin on `range` / `bay` — waiting-yard `lobby` is the same `fire()`. Do not force shark-fin when `S.desktop`. Pinch is not a trigger (`pinchStrength` is the shark-fin safe gate only). Reload is index+middle ceiling (`maybeReloadGesture` after shark-fin, before `updateAim`) — rising-edge refill, no fire, no phase re-gate, no DESKTOP force.
- Do not let leftover JOIN/CODE eat waiting-Yard HID after a live join. `muteJoinPad` blurs the field and drops `join-mute` so the hidden cursor is not a pad mute. WARM UP / ENTER RANGE / LEAVE stay chrome. Create / leave re-arm JOIN.
