# SABLE engine lock — 2026-09-04

**Web-Native Core (WebGL / 3D Canvas / Web Workers) + Gemini 3.8 Flash Agentic SWE.**

- **Dropped**: Godot 4.7 (toolchain bloat, GDExtension compile friction, OS capture fragmentation) and Unity (proprietary editor lock-in, 50 GB footprint).
- **Target Architecture**: Pure web-native client (`proto/` and web client) running 60–120 FPS in modern browsers.
- **Tracking**: In-browser `getUserMedia` (720p/1080p), NCC patch tracking, One Euro pointing filter, sub-millisecond DOM HID click mailbox.
- **Modes**:
  - **Range**: 60s physical ADS arcade target practice loop.
  - **Bay 1v1**: CANCHO duel arena with 3D perspective projection, WASD pad movement, physical lift ADS, window/angle cover vs open middle exposure, and operator styles (`default`, `ranked`, `night`).
- **Floor**: 1080p60 on integrated graphics / GTX 1650; 1080p120 on modern GPUs. Zero install required.
- **Repo**: https://github.com/lyffseba/sable (private)
